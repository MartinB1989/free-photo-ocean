import React from 'react'
import '../assets/style/components/pagination.css'
const Pagination = () => {
  /*
    totalItems(Array)
    hasPrev(bool)
    hasNext(bool)
    currentPage(number)
    nextPage(number)
    prevPage(number)
    totalPages(number)
    goTo(function)
  */
  return (
    <div>Pagination</div>
  )
}

export default Pagination